\section{DFA}
\label{sec:dfa}

\begin{tikzpicture}[->,>=stealth',shorten >=1pt,auto,node distance=4.8cm,
                    semithick]
  \tikzstyle{every state}=[fill=red,draw=none,text=white]

  \node[initial,state] (A)                    {$q_a$};
  \node[state]         (B) [right of=A] {$q_b$};

  \node[state]         (C) [ right of=B] {$q_c$};
    \node[state]         (D) [below right of=C] {$q_d$};
  \node[state]         (E) [below  of=D] {$q_e$};
  \path (A) edge              node {CON} (B)
        (B) edge  [bend left=80]node {LOGIN} (C)
           
        (C) edge [bend left=80] node {USE\_CAR} (D)
            edge   node {Err} (B)
            edge [bend right]  node {LOGIN\_SEQ} (B)
         
        (D) edge [bend right] node {CMD} (E)
            edge              node {Err} (C)
            edge     [bend left = 60]         node {USE\_CAR\_SEQ}(C)
            edge [bend left]  node {FINALIZE} (A)
        (E) edge [bend left = 80] node {CMD\_SEQ} (D)
            edge [bend right =90] node {Err} (D);
            
\end{tikzpicture}

Our \textsf{DFA} is formally defined as
\[Q = \{q_{a}, q_{b},q_{c},q_{d},q_{e}\}\]
where $q_{a}$ is start state in which server accepts conncection requests from clients. $q_{b}$ is state where client sends its password to login to the server. $q_{c}$ is the state in which server gets requests to connect to a car. $q_{d}$ is the state in which client's request to the server for a specific car is satisfied and commands for controlling utilities are received. $q_{e}$ 



    \begin{sequencediagram}
        \newthread[white]{c}{Client}
        \newinst[4]{s}{CSS Server}

        \mess{c}{CON}{s}
        \mess{s}{CONSEQ}{c}
        \mess{c}{LOGIN}{s}
        \mess{s}{LOGINSEQ}{c}


        
        \mess{c}{USECAR}{s}
        \mess{s}{USECARSEQ}{c}



        
        \mess{c}{CMD}{s}
        \mess{s}{CMDSEQ}{c}

        
        \mess{c}{FINALIZE}{s}
        \mess{s}{FINALIZESEQ}{c}

       
    \end{sequencediagram}

    
